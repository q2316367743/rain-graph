import MessageUtil from "@/utils/MessageUtil"

export const preload = {
    openFile() {
        MessageUtil.warning("请前往utools体验此功能");
    },
    openFileToString() {
        MessageUtil.warning("请前往utools体验此功能");
    }
}

export const api = {
    backup: {
        listBackup() {
            MessageUtil.warning("请前往utools体验此功能");
        },
        writeBackup() {
            MessageUtil.warning("请前往utools体验此功能");
        },
        removeBackup() {
            MessageUtil.warning("请前往utools体验此功能");
        }
    }
}